import colors from '../../variables/colors';
const MuiDialogActionsStyled = (defaultTheme, modifyTheme) => ({
  root: {
    padding: defaultTheme.spacing(2.5),
  },
});

export default MuiDialogActionsStyled;
