import colors from '../../variables/colors';
const MuiDialogTitleStyled = (defaultTheme, modifyTheme) => ({
  root: {
    margin: 0,
    padding: defaultTheme.spacing(2.5),
  },
});

export default MuiDialogTitleStyled;
