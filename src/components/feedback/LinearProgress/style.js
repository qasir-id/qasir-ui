const MuiLinearProgressStyled = (defaultTheme, modifyTheme) => ({
  colorPrimary: {
    color: modifyTheme.palette.primary.main,
  },
  colorSecondary: {
    color: modifyTheme.palette.secondary.main,
  },
});

export default MuiLinearProgressStyled;
