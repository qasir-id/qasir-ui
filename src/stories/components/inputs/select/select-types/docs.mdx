import { Preview, Story } from '@storybook/addon-docs/blocks';

<h1>Type Selects</h1>
<p style={{fontSize: 18, fontFamily: ['"Montserrat"', 'sans-serif'].join(',')}}>Menus are positioned over their emitting elements such that the currently selected menu item appears on top of the emitting element.</p>

<Preview withSource="none">
  <Story id="components-inputs-select-example--types" />
</Preview>

```js
import FormControl from '@material-ui/core/FormControl';
import InputLabel from '@material-ui/core/InputLabel';
import MenuItem from '@material-ui/core/MenuItem';

import Select from 'qasir-ui/inputs/Select';
```

```html
<FormControl variant="outlined">
  <InputLabel>Simple Select</InputLabel>
    <Select
      label="Simple Select"
      inputProps={{
        name: 'simple-select',
      }}
    >
      <MenuItem value="">
        <em>None</em>
      </MenuItem>
      <MenuItem value={10}>Ten</MenuItem>
      <MenuItem value={20}>Twenty</MenuItem>
      <MenuItem value={30}>Thirty</MenuItem>
    </Select>
</FormControl>
```

```html
<FormControl variant="outlined">
  <InputLabel>Native Select</InputLabel>
    <Select
      native
      label="Native Select"
      inputProps={{
        name: 'native-select',
      }}
    >
      <MenuItem value="">
        <em>None</em>
      </MenuItem>
      <MenuItem value={10}>Ten</MenuItem>
      <MenuItem value={20}>Twenty</MenuItem>
      <MenuItem value={30}>Thirty</MenuItem>
    </Select>
</FormControl>
```

