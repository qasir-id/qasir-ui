import { Subtitle, Preview, Story } from '@storybook/addon-docs/blocks';

<h1>Form Props</h1>
<p style={{fontSize: 18, fontFamily: ['"Montserrat"', 'sans-serif'].join(',')}}>Standard form attributes are supported e.g. <code>required</code>, <code>disabled</code>, type, etc. as well as a <code>helperText</code> which is used to give context about a field’s input, such as how the input will be used.</p>

<Preview withSource="none">
  <Story id="components-inputs-textfield-example--form-props" />
</Preview>

```js
import TextField from 'qasir-ui/inputs/TextField';
```

```html
<TextField required label="Required*" defaultValue="Hello World" variant="outlined" />
```
