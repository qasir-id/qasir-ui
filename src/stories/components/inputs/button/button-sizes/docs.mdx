import { Preview, Story } from '@storybook/addon-docs/blocks';

<h1>Sizes Buttons</h1>
<p style={{fontSize: 18, fontFamily: ['"Montserrat"', 'sans-serif'].join(',')}}>Fancy larger or smaller buttons? Use the size property.</p>

<Preview withSource="none">
  <Story id="components-inputs-button-example--sizes" />
</Preview>

```js
import Button from 'qasir-ui/inputs/Button';
```

```html
<Button variant="contained" color="primary" size="large">Large</Button>
```

```html
<Button variant="contained" color="primary">Medium</Button>
```

```html
<Button variant="contained" color="primary" size="small">Small</Button>
```
