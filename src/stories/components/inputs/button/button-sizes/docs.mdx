import { Subtitle } from '@storybook/addon-docs/blocks';
import Button from 'components/inputs/Button';

# Sizes Buttons
<Subtitle>Fancy larger or smaller buttons? Use the size property.</Subtitle>

### Import

```js
import Button from 'qasir-ui/inputs/Button';
```

### Usage

<Button variant="contained" color="primary" size="large" style={{marginTop: 20}}>Large</Button>

```html
<Button variant="contained" color="primary" size="large">Large</Button>
```

<Button variant="contained" color="primary">Medium</Button>

```html
<Button variant="contained" color="primary">Medium</Button>
```

<Button variant="contained" color="primary" size="small">Small</Button>

```html
<Button variant="contained" color="primary" size="small">Small</Button>
```
