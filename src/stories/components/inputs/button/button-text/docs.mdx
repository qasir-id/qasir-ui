import { Preview, Story } from '@storybook/addon-docs/blocks';

<h1>Text Buttons</h1>
<p style={{fontSize: 18, fontFamily: ['"Montserrat"', 'sans-serif'].join(',')}}>Text buttons are typically used for less-pronounced actions, including those located in dialogs & in cards In cards, text buttons help maintain an emphasis on card content.</p>

<Preview withSource="none">
  <Story id="components-inputs-button-example--text" />
</Preview>

```js
import Button from 'qasir-ui/inputs/Button';
```

```html
<Button variant="text">Text</Button>
```
