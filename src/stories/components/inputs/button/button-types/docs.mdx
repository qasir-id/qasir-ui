import { Preview, Story } from '@storybook/addon-docs/blocks';
import Button from 'components/inputs/Button';

<h1>Type Buttons</h1>
<p style={{fontSize: 18, fontFamily: ['"Montserrat"', 'sans-serif'].join(',')}}>There are text, contained, and toggle buttons. Text buttons are typically used for less important actions.</p>

<Preview withSource="none" withToolbar>
  <Story id="." />
</Preview>

```js
import Button from 'qasir-ui/inputs/Button';
```

<Button variant="contained" color="primary">Primary</Button>

```html
<Button variant="contained" color="primary">Primary</Button>
```

```html
<Button variant="contained" color="secondary">Secondary</Button>
```

```html
<Button variant="contained" disabled>Disable</Button>
```

```html
<Button variant="outlined">Outline</Button>
```

```html
<Button variant="text">Text</Button>
```
