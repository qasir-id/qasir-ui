import { Preview, Story } from '@storybook/addon-docs/blocks';

<h1>Full Width Buttons</h1>
<p style={{fontSize: 18, fontFamily: ['"Montserrat"', 'sans-serif'].join(',')}}>The buttons will take up the full width of its container.</p>

<Preview withSource="none">
  <Story id="components-inputs-button-example--full-width" />
</Preview>

```js
import Button from 'qasir-ui/inputs/Button';
```

```html
<Button variant="contained" color="primary" fullWidth>Full Width</Button>
```
