import { Subtitle } from '@storybook/addon-docs/blocks';
import ButtonFullWidth from './index';

# Full Width Buttons
<Subtitle>The buttons will take up the full width of its container.</Subtitle>

### Import

```jsx
import Button from 'qasir-ui/inputs/Button';
```

### Usage

<ButtonFullWidth style={{marginTop: 20}} />

```jsx
<Button variant="contained" color="primary" fullWidth>Full Width</Button>
```
