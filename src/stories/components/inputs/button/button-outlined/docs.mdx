import { Preview, Story } from '@storybook/addon-docs/blocks';

<h1>Outlined Buttons</h1>
<p style={{fontSize: 18, fontFamily: ['"Montserrat"', 'sans-serif'].join(',')}}>Outlined buttons are medium-emphasis buttons. They contain actions that are important, but aren’t the primary action in an app.</p>

<Preview withSource="none">
  <Story id="components-inputs-button-example--outlined" />
</Preview>

```js
import Button from 'qasir-ui/inputs/Button';
```

```html
<Button variant="outlined">Outline</Button>
```
