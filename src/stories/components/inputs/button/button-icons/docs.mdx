import { Preview, Story } from '@storybook/addon-docs/blocks';

<h1>Icons Buttons</h1>
<p style={{fontSize: 18, fontFamily: ['"Montserrat"', 'sans-serif'].join(',')}}>Icon buttons are commonly found in app bars and toolbars. Icons are also appropriate for toggle buttons that allow a single choice to be selected or deselected, such as adding or removing a star to an item.</p>

<Preview withSource="none">
  <Story id="components-inputs-button-example--icons" />
</Preview>

```js
import Button from 'qasir-ui/inputs/Button';
```

```html
<Button variant="text">Text</Button>
```
