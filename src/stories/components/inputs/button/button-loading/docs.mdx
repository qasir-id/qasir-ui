import { Subtitle, Preview, Story } from '@storybook/addon-docs/blocks';

# Loading Buttons

<Subtitle>
  Display a loading indicator whenever the user clicks on the Load button, and hide it once the corresponding ajax call has been completed.
</Subtitle>
<Preview withSource="none">
  <Story id="components-inputs-button-example--loading" />
</Preview>

```js
import Button from 'qasir-ui/components/input/button';
```

```html
<Button variant="text">Text</Button>
```
