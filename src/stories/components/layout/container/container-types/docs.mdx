import { Title, Subtitle } from '@storybook/addon-docs/blocks';
import {ContainerFluid, ContainerFixed} from './index';

<Title>Container Types</Title>
<Subtitle>Set the max-width to match the min-width of the current breakpoint. This is useful if you'd prefer to design for a fixed set of sizes instead of trying to accommodate a fully fluid viewport. It's fluid by default.</Subtitle>

### Import

```jsx
import Container from 'qasir-ui/inputs/Container';
```

### Usage

<ContainerFluid />

```jsx
<Container>...</Container>
```

<ContainerFixed />

```jsx
<Container fixed>...</Container>
```
