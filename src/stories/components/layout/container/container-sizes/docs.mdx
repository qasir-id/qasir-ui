import { Preview, Story } from '@storybook/addon-docs/blocks';

<h1>Container Sizes</h1>
<p style={{fontSize: 18, fontFamily: ['"Montserrat"', 'sans-serif'].join(',')}}>Determine the max-width of the container. The container width grows with the size of the screen. Set to false to disable maxWidth.</p>

<Preview withSource="none">
  <Story id="components-layout-container-example--sizes" />
</Preview>

```jsx
import Container from 'qasir-ui/inputs/Container';
```

```jsx
<Container maxWidth="xl">...</Container>
```

```jsx
<Container maxWidth="lg">...</Container>
```

```jsx
<Container maxWidth="md">...</Container>
```

```jsx
<Container maxWidth="sm">...</Container>
```

```jsx
<Container maxWidth="xs">...</Container>
```
