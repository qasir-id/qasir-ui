import { Title, Subtitle } from '@storybook/addon-docs/blocks';
import GridFluidBasic from './index';

<Title>Fluid basic</Title>
<Subtitle>Fluid grids use columns that scale and resize content. A fluid grid’s layout can use breakpoints to determine if the layout needs to change dramatically. The column widths apply at all breakpoints (i.e. xs and up).</Subtitle>

### Import

```jsx
// Vendors
import React from 'react';

// Material UI
import { makeStyles } from '@material-ui/core/styles';

// Qasir UI
import Box from 'qasir-ui/layout/Box';
import Grid from 'qasir-ui/layout/Grid';
```

<h3 style={{marginBottom: 25}}>Usage</h3>

<GridFluidBasic />

```jsx
const useStyles = makeStyles((theme) => ({
  root: {
    flexGrow: 1,
  },
}));

const defaultProps = {
  bgcolor: '#cfe8fc',
  p: 2,
  textAlign: 'center',
};

const GridFluidBasic = () => {
  const classes = useStyles();

  return (
    <div className={classes.root}>
      <Grid container spacing={3}>
        <Grid item xs={12}>
          <Box {...defaultProps}>xs=12</Box>
        </Grid>
        <Grid item xs={6}>
          <Box {...defaultProps}>xs=6</Box>
        </Grid>
        <Grid item xs={6}>
          <Box {...defaultProps}>xs=6</Box>
        </Grid>
        <Grid item xs={3}>
          <Box {...defaultProps}>xs=3</Box>
        </Grid>
        <Grid item xs={3}>
          <Box {...defaultProps}>xs=3</Box>
        </Grid>
        <Grid item xs={3}>
          <Box {...defaultProps}>xs=3</Box>
        </Grid>
        <Grid item xs={3}>
          <Box {...defaultProps}>xs=3</Box>
        </Grid>
      </Grid>
    </div>
  );
};

export default GridFluidBasic;
```
