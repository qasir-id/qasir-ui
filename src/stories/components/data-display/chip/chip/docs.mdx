import { Subtitle, Preview, Story } from '@storybook/addon-docs/blocks';

# Chip

<Subtitle>
  Chips are compact elements that represent an input, attribute, or action.
</Subtitle>
<Preview withSource="none">
  <Story id="components-data-display-chip-example--chip" />
</Preview>

### Import
```js
import Chip from 'qasir-ui/data-display/Chip';
```

### Usage

```html
      <Chip label="Basic" />
```
```html 
      <Chip label="Disabled" disabled />
```
### Function HanldeClick for example Usage
```html
  const handleClick = () => {
    alert('You clicked the Chip.');
  };

  const handleDelete = () => {
    alert('You clicked the delete icon.');
  };

```

```html
      <Chip avatar={<Avatar>M</Avatar>} label="Clickable" onClick={handleClick} />
```
```html  
      <Chip
        avatar={<Avatar alt="Natacha" src="https://static.toiimg.com/photo/msid-67586673/67586673.jpg?3918697" />}
        label="Deletable"
        onDelete={handleDelete}
      />
```
```html
      <Chip
        icon={<FaceIcon />}
        label="Clickable deletable"
        onClick={handleClick}
        onDelete={handleDelete}
      />
```
```html
      <Chip
        label="Custom delete icon"
        onClick={handleClick}
        onDelete={handleDelete}
        deleteIcon={<DoneIcon />}
      />
```
```html
      <Chip label="Clickable Link" component="a" href="#chip" clickable />
```
```html 
      <Chip
        avatar={<Avatar>M</Avatar>}
        label="Primary clickable"
        clickable
        color="primary"
        onDelete={handleDelete}
        deleteIcon={<DoneIcon />}
      />
```
```html
      <Chip
        icon={<FaceIcon />}
        label="Primary clickable"
        clickable
        color="primary"
        onDelete={handleDelete}
        deleteIcon={<DoneIcon />}
      />
```
```html
      <Chip label="Deletable primary" onDelete={handleDelete} color="primary" />
```

```html  
      <Chip
        icon={<FaceIcon />}
        label="Deletable secondary"
        onDelete={handleDelete}
        color="secondary"
      />
```